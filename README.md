clone linux biar ga bentrok sama tuckr:
git clone git@github.com:muhammadhabibies/dev-nvim-linux.git ~/.config/nvim

copy tmux conf ke satu atas diatas directory saat ini:
cp .tmux.conf ../

prerequisites
ripgrep (rg --version)
nodejs: prettierd
